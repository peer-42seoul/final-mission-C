const Page: React.FC<{ params: { id: string } }> = (props) => {
  return (
    <div
      style={{ height: "100vh", width: "100vw", backgroundColor: "#000" }}
    ></div>
  );
};

export default Page;
